#include <vector>
#include <iostream>

int main()
{
    std::vector < int > wektor;
   
    for( int i = 1; i <= 16; i *= 2 )
         wektor.resize( i, i );
   
    for( int i = 0; i < wektor.size(); ++i )
         std::cout << wektor[ i ] << ' ';
   
}
